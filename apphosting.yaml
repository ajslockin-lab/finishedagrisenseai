# Settings to manage and configure the app hosting backend.
# Previously used with Firebase App Hosting.

runConfig:
  # Increase this value if you'd like to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 1
